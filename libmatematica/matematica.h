/**
 * Calcula o seno de um ângulo, definido em radianos.
 * 
 * @param angulo_radianos Um valor que representa o ângulo, em radianos, a ser calculado o seno.
 * @return Um valor referente ao seno do ângulo fornecido.
*/
float calcular_seno(float angulo_radianos);

/**
 * Calcula o cosseno de um ângulo, definido em radianos.
 * 
 * @param angulo_radianos Um valor que representa o ângulo, em radianos, a ser calculado o cosseno.
 * @return Um valor referente ao cosseno do ângulo fornecido.
*/
float calcular_cosseno(float angulo_radianos);

/**
 * Calcula a tangente de um ângulo, definido em radianos.
 * 
 * @param angulo_radianos Um valor que representa o ângulo, em radianos, a ser calculado a tangente.
 * @return Um valor referente à tangente do ângulo fornecido.
*/
float calcular_tangente(float angulo_radianos);